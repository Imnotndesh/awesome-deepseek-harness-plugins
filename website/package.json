{
  "name": "awesome-dsh-plugins-site",
  "dependencies": {
    "@astrojs/check": "^0.9.10",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.2",
    "typescript": "^7.0.2"
  },
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "bun run build && wrangler pages deploy dist --project-name awesome-deepseek-harness-plugins --branch main"
  },
  "type": "module"
}
